# PostgreSQL connection
DATABASE_URL=postgresql+psycopg2://postgres:root@localhost:5432/decision_replay

# JWT
SECRET_KEY=Q7v9mK2xR8pL4nT6wY3cF1sA9dE5hJ8z
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60
REFRESH_TOKEN_EXPIRE_DAYS=7

# App
PROJECT_NAME=Expert Decision Replay Platform
ENV=development

# CORS - comma-separated list of allowed frontend origins
ALLOWED_ORIGINS=http://localhost:5173

# Bootstrap administrator (used only by scripts/seed_admin.py)
# Change SEED_ADMIN_PASSWORD before running the seed script, and change it
# again via the app immediately after first login.
SEED_ADMIN_EMAIL=admin@decisionreplay.example.com
SEED_ADMIN_PASSWORD=ReplayAdmin#2026Secure!
SEED_ADMIN_FULL_NAME=System Administrator
